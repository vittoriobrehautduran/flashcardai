# Amplify Hosting build for Next.js SSR (platform WEB_COMPUTE).
version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        # Amplify console env vars only exist during the build, not on the
        # SSR server that handles requests. Write the ones the server needs
        # into .env.production so Next.js loads them at runtime.
        # (|| true keeps the build alive if a var group isn't set yet.)
        # Note: Gemini keys are per-user in Settings — do not ship GEMINI_API_KEY.
        - env | grep -e NEON_CONNECTION_STRING -e COGNITO_ -e SETTINGS_SECRET -e GEMINI_MODEL -e APP_URL >> .env.production || true
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
