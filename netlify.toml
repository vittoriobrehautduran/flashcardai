# Netlify build settings for the Next.js app.
# The official plugin turns API routes and middleware into Netlify functions.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
